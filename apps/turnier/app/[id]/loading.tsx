import { LoadingScreen } from "@/components/ui/LoadingScreen";

/** Route-Loading für eine einzelne Turnier-Detailseite. */
export default function Loading() {
  return <LoadingScreen label="Turnier wird geladen…" />;
}
